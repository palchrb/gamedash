{
  "_comment_nodes": "Optional. If absent, a single 'local' node is synthesized from UFW_SIDECAR_URL + DOCKER_HOST env vars. For multi-node: add remote nodes here. dockerHost present = use docker CLI directly (local); absent = go via sidecar HTTP (remote).",
  "nodes": {
    "local": {
      "sidecarUrl": "http://ufw-sidecar:9090",
      "sidecarToken": "your-local-sidecar-token",
      "dockerHost": "tcp://docker-proxy:2375"
    },
    "_example_remote": {
      "sidecarUrl": "http://game-node.tailnet:9090",
      "sidecarToken": "your-remote-sidecar-token",
      "label": "Game VPS (Amsterdam)"
    }
  },
  "services": [
    {
      "id": "mc1",
      "name": "Minecraft",
      "type": "minecraft",
      "container": "minecraft-mc-1",
      "rcon": {
        "host": "mc",
        "port": 25575,
        "passwordEnv": "RCON_PASSWORD"
      },
      "ports": [
        { "port": "25565", "proto": "tcp" },
        { "port": "19132", "proto": "udp" },
        { "port": "24454", "proto": "udp" }
      ],
      "connectAddress": "play.example.com",
      "mapUrl": "https://map.example.com",
      "mapProxy": {
        "host": "bluemap",
        "port": 8100,
        "scheme": "http"
      },
      "dataDir": "/mc-data"
    },
    {
      "id": "impostor",
      "name": "Among Us",
      "type": "impostor",
      "container": "impostor",
      "connectAddress": "play.example.com:22023",
      "connectHelper": {
        "type": "impostor",
        "scheme": "http",
        "name": "My Server"
      },
      "_comment_admin_api": "Requires the Impostor.Plugins.AdminApi plugin running on the Impostor server. Defaults to http://<container>:8081 — override with impostorAdminApiUrl. Set impostorAdminApiKey to match the X-Admin-Key configured in the plugin (leave empty if AdminApi is bound to localhost only). impostorShowPrivateGames (default false): private lobbies are hidden from the PWA lobby list by default; set to true in a family setup where siblings should see each other's private games.",
      "impostorAdminApiUrl": "http://impostor:8081",
      "impostorShowPrivateGames": false,
      "ports": [
        { "port": "22023", "proto": "udp" }
      ]
    },
    {
      "id": "terraria",
      "name": "Terraria",
      "type": "tshock",
      "container": "terraria",
      "connectAddress": "play.example.com:7777",
      "_comment_tshock": "Configure an Application REST Token in tshock config.json — runtime /v2/token/create tokens don't survive restart. Token name must be hard to guess.",
      "tshockApiToken": "your-application-rest-token-here",
      "ports": [
        { "port": "7777", "proto": "tcp" }
      ]
    }
  ]
}
